description = "List all available sound packs from the PeonPing registry."
prompt = """
Below is a list of available sound packs. Please display them to the user in a nice table or list.

!{powershell.exe -NoProfile -ExecutionPolicy Bypass -File ${extensionPath}/scripts/manage.ps1 -Action list}
"""
